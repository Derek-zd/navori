# Navori — 待办与占位符跟踪

> 集中跟踪待办、占位符、遗留问题；每项标注优先级 + 目标版本。

---

## 一、待办

| # | 项 | 优先级 | 目标版本 |
|---|---|---|---|
| D1 | docs/TESTING.md（测试策略 + 用例清单） | P1 | M0 |
| D2 | docs/MILESTONES.md（里程碑验收清单细化） | P1 | M0 |
| D3 | docs/EDGE-CASES.md（边界场景 + 错误码） | P1 | M0 |
| D4 | SSH 部署目标（deploy_targets.type=ssh，schema 已预留） | P2 | v2 |
| D5 | gRPC agent（横向扩容 + 裸机执行） | P2 | v2 |
| D6 | 多用户 RBAC 细化 / 多级审批流 | P2 | v2 |
| D7 | 多实例调度（任务队列/选主） | P2 | v2 |
| D8 | 导出/导入 YAML 配置 | P3 | v1.5 |
| D9 | 资源状态检测定时任务（registry/deploy_target 自动测试） | P1 | M7/M8 |
| D10 | 流水线 cron 定时触发 | P1 | v1.5 |
| D11 | 定时触发 + 有新 commit 才跑 | P2 | v2 |
| D16 | 完整审计中心（按资源筛选、详情、导出） | P2 | 后续 |

---

## 二、占位符（MVP 前必须替换/实现）

| # | 位置 | 说明 |
|---|---|---|
| P1 | 构建/部署接口 | v1 用 docker/kubectl shell 实现，须接口化（buildx/deploy 包），v2 换 agent 实现 |
| P2 | 通知重试 | 固定 3 次指数退避，暂不可配 |

---

## 四、UI 评审反馈（2026-08-14，已于 2026-08-14 处理）

| # | 问题 | 状态 | 处理 |
|---|---|---|---|
| U1 | 审批点「同意/拒绝」无反馈，切页回来才消失 | ✅ 已修复 | 按钮 loading 态 + 乐观移除当前行 + toast |
| U2 | 命名空间挂在部署目标上不合理（一集群多 namespace） | ✅ 已修复 | namespace 移到流水线 deploy 配置（前端表单已暴露 deploy.namespace）；部署目标表单已移除 namespace 输入 |
| U3 | workload 类型只有 Deployment | ✅ 已修复 | deploy 配置加 kind 下拉（Deployment/StatefulSet/DaemonSet/Job/CronJob） |
| U4 | 变量混在设置页 | ✅ 已修复 | 变量独立成导航项 /variables；设置页只留用户管理 |
| U5 | 新增仓库/流水线/部署目标等内联表单太丑 | ✅ 已修复 | 全部改为 Modal 弹窗式：仓库/流水线/镜像仓库/部署目标/变量/用户均支持弹窗新增 + 行内删除，流水线支持编辑 |

## 三、已决议

- 无 DSL：UI 表单配置（v1）
- 放弃 Jenkinsfile / 任意流水线 DSL
- 仓库无关：webhook 支持通用格式 + GitLab 格式
- 单实例：SQLite / MySQL 均单实例；横向扩容走 v2 agent
- 构建器：docker/podman（非 BuildKit / Kaniko）
- 部署：kubectl（非 client-go，v1）


---

## 五、UX 优化路线图（2026-08-17 调研，来源 docs/UX-RESEARCH.md）

> 已整合进 MILESTONES.md：P0 → M6，P1 → M7，P2 → M8（暂缓）。

| 优先级 | 项 | 目标里程碑 | 说明 |
|---|---|---|---|
| P0 | Run 详情日志体验 | M6.1 | step 分组/折叠、搜索、仅看失败、复制日志、步骤时间线 |
| P0 | 审批嵌入 Run 详情 | M6.2 | deploy/approve 步骤处直接审批，保留待审批页 |
| P0 | Dashboard 概览化 | M6.3 | 统计卡片 + 最近运行 + 筛选 |
| P0 | 空状态引导 | M6.4 | 空列表主按钮 + 引导 |
| P0 | 统一状态与色彩体系 | M6.5 | Status 扩展 waiting/healthy/degraded |
| P1 | 部署环境页面升级 | M7.1 | 当前镜像/部署历史/回滚入口 |
| P1 | Run 详情 Tabs 化 | M7.2 | 概览/变更/参数/日志/部署 |
| P1 | 流水线表单增强 | M7.3 | tag 预览/校验/分支排序 |
| P1 | 环境变量分级 | M7.4 | 流水线级覆盖全局变量 |
| P1 | 列表筛选与搜索 | M7.5 | 分支/状态/关键词 |
| P2 | 凭证聚合视图 | M8 | 统一凭证页，保留归属 |
| P2 | 价值流视图（VSM） | M8 | 端到端交付时间线 |
| P2 | 项目/多租户空间 | M8 | 多团队隔离 |
| P2 | 流水线可视化编辑器 | M8 | 步骤卡片 + 连线 |
| P2 | Webhook/通知测试工具 | M8 | 发送测试事件 |


---

## 六、已处理（2026-08-17）

- 镜像仓库测试报错：`docker login` 不再写入系统凭据存储，改用临时 DOCKER_CONFIG + `--password-stdin`。
- 增加凭证管理：环境变量页升级为「环境变量与凭证」，支持 Git HTTPS/SSH 和镜像仓库凭证。
- 代码仓库支持选择 Git 凭证；镜像仓库支持选择已有凭证或直接用户名/密码。
- 流水线构建推送时自动使用镜像仓库配置登录（临时 DOCKER_CONFIG），不再依赖手动 docker login。


---

## 七、已处理（2026-08-17 第二轮）

- 镜像仓库测试进一步加固：写入临时空 docker config.json，避免继承 macOS 钥匙串。
- 镜像仓库编辑/新增弹窗增加「测试连接」；列表增加状态灯（绿/红/灰）。
- 代码仓库/镜像仓库的凭证下拉支持「新建」，创建后自动选中并同步到环境变量与凭证页。
- 代码仓库移除手动默认分支，扫描时自动探测仓库默认分支并保存。


---

## 八、已处理（2026-08-17 第三轮）

- 部署环境增加测试状态显示（`last_test_status` / `last_test_at`，与镜像仓库一致）。
- Toast 提示移到页面顶部，更易发现。
- 定时任务与状态检测设计已写入 docs/DESIGN.md §9.1，待实施。


---

## 九、已处理（2026-08-17 第四轮）

- SPA history fallback：刷新 `/repositories`、`/runs/:id` 等前端路由不再 404。
- 流水线列表增加状态列（未运行/最近结果），新增流水线详情页 `/pipelines/:id`。
- 构建 Docker 配置隔离最终方案：`DOCKER_CONFIG` 指向 `DATA_DIR/docker-config`（空 auths），不再设置 `BUILDX_CONFIG`；公共镜像拉取/构建正常，不触发系统钥匙串。
- 验证：流水线构建/推送/审批/部署到 K8s 全链路跑通。


---

## 十、已处理（2026-08-18 上午）

- 查看日志 → 查看详情（Dashboard / PipelinesDetail）。
- pull 步骤输出 git 拉取日志与 HEAD commit。
- approve 步骤记录审批人、时间、决策（通过/拒绝，拒绝含原因）。
- 待审批页改为「审批中心」，增加审批历史/审计记录。


---

## 十一、已处理（2026-08-18 上午第二轮）

- M7.4 环境变量分级：流水线级变量覆盖全局变量，tag 模板引用；表单展示最终生效变量。
- M7.5 列表筛选与搜索：Dashboard 支持状态/触发方式筛选+关键词；仓库、流水线支持关键词搜索。


---

## 十二、已处理（2026-08-18）

- M7.1 部署环境页面升级：列表显示最近部署，新增环境详情页与部署历史，支持回滚到历史成功镜像（后端异步执行、写审计、仅在配置命名空间内操作）。

---

## 十三、已处理（2026-08-18）

- 流水线新增「分组」字段与列表分组展示（作为应用/产品雏形，后续可升级为 Application 实体）。
- 撤掉部署环境详情的回滚按钮及后端 rollback 接口。方向决策：部署历史/回滚不属于「部署环境」，应归属「应用/流水线」；短期用「重跑目标 commit」做回滚，后续在应用/版本管理里做正式回滚（切历史镜像）。

---

## 十四、已处理（2026-08-18 下午）

- M7.3 流水线表单增强：tag 模板实时预览、镜像名校验、分支规则上移/下移排序。
- M7.2 Run 详情 Tabs 化：概览（状态/步骤/审批）与日志两个 Tab。
- M7 全部验收完成（M7.1–M7.5）。

---

## 十五、已处理（2026-08-18 下午第二轮）

- D9 资源状态检测定时任务：每 5 分钟自动测试镜像仓库/部署环境，刷新状态灯。
- D10 流水线 cron 定时触发：流水线可配 5 字段 cron，调度器每分钟检查并触发。

---

## 十六、已处理（2026-08-18 下午第三轮）

- D11 定时触发增强：cron 触发前先 git ls-remote 对比默认分支 HEAD，与上次 cron run 相同则跳过，零空跑。

---

## 十七、已处理（2026-08-18）

- 通知模块：NotifyChannel 模型 + 通道管理页（REST/邮件/飞书/钉钉/企微）；notify 包扩展多通道 sender；流水线可选用通道与触发事件。

---

## 十八、已处理（2026-08-18）

- 通知增强：发件邮箱移至系统设置；审批通知（approval.requested/approved/rejected）；飞书/钉钉/企微 markdown 消息格式。

---

## 十九、已处理（2026-08-18）

- 新增前端操作审计查看页（设置 → 操作审计，按操作类型筛选）。V1 功能面全部完成。

## V1 状态
- V1 里程碑与验收全部完成。剩余为后续版本项（v1.5：导入导出、完整审计中心；v2：SSH/gRPC agent/多实例/RBAC）。
